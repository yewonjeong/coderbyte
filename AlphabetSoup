def AlphabetSoup(str)

  # code goes here
  return str.gsub(/\W+/,"").split("").sort.join
end
   
# keep this function call here 
# to see how to enter arguments in Ruby scroll down   
AlphabetSoup(STDIN.gets) 
